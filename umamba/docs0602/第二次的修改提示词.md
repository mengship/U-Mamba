请做最后一个小修，只处理测试脚本可运行性问题，不要再改核心模块逻辑。

当前文件：
- `umamba/docs0602/script/test_rthd_v1_enhancements.py`

问题：
直接运行
`python3 umamba/docs0602/script/test_rthd_v1_enhancements.py`
会报：
`ModuleNotFoundError: No module named 'nnunetv2'`

原因是脚本当前导入写的是：
`from nnunetv2.nets.rthd_modules import ...`
但当前仓库结构下，这个脚本不是从正确包根启动的。

请修复测试脚本导入方式，使它能在当前仓库根目录下直接运行。

要求：
1. 不改核心模块逻辑，只改测试脚本导入和必要的 `sys.path` 处理。
2. 让以下命令可运行：
   `python3 umamba/docs0602/script/test_rthd_v1_enhancements.py`
3. 修改后自行运行：
   `python3 -m py_compile umamba/docs0602/script/test_rthd_v1_enhancements.py`
   `python3 umamba/docs0602/script/test_rthd_v1_enhancements.py`
4. 输出你改了什么，以及测试结果。