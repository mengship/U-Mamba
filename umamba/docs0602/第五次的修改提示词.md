把下面这段直接给 Claude Code 就行：

```md
请做第五次小修，这次不要改 `RTHD` 核心逻辑，只修测试脚本的 device 适配问题。

## 任务背景

当前真实 `SS2D` 已经可以成功导入，远程日志显示：

- `✅ Successfully imported SS2D from /home/wang/U-Mamba/umamba/instructions`

但是测试运行时又报：

```text
RuntimeError: Expected u.is_cuda() to be true, but got false
```

这说明：

- 当前测试脚本创建的输入 tensor 在 CPU 上
- 但真实 `vmamba / SS2D` 路径内部调用了 CUDA 版本的 selective scan
- 所以测试脚本需要做 device 适配

---

## 本次只改这些文件

主要修改：
- `umamba/docs0602/script/test_rthd_v1_enhancements.py`

如非必要，不要改：
- `umamba/nnunetv2/nets/rthd_modules.py`
- `umamba/instructions/vmamba.py`
- `umamba/nnunetv2/nets/UMambaEnc_RTHD.py`

这次目标是修测试脚本，不是修核心模块。

---

## 修改目标

让测试脚本在以下两种环境下都能合理工作：

### 情况 1：有 CUDA
- 模型放到 CUDA
- 输入 tensor 放到 CUDA
- 正常测试真实 `SS2D` 路径

### 情况 2：没有 CUDA
- 不要直接崩掉
- 可以选择：
  - 跳过依赖真实 `SS2D/CUDA` 的测试
  - 或明确降级到 fallback 路径测试
- 但必须给出清晰提示，说明为什么跳过

---

## 推荐修改方式

### 1. 在测试脚本开头统一定义 device

例如：

```python
device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
print(f"Using device: {device}")
```

### 2. 所有输入 tensor 放到 device 上

例如：

```python
x = torch.randn(B, C, D, H, W, device=device)
```

以及 axial/coronal/sagittal 这些测试张量也都放到 device。

### 3. 所有 block / module 也放到 device 上

例如：

```python
block = TriViewVMambaBlock(...).to(device)
```

### 4. 如果某些测试明确依赖真实 `SS2D` CUDA 路径，请在没有 CUDA 时跳过

可以写成：

```python
if not torch.cuda.is_available():
    print("Skipping real SS2D tests because CUDA is not available")
    return
```

或者更细一点：
- `TriViewReconstruction` 这类纯 PyTorch 模块可以继续测
- `TriViewVMambaBlock` / `RTHDBlock` 这类会走真实 `SS2D` 的，在无 CUDA 时跳过或 fallback

---

## 重要要求

### 1. 不要改第一版增强逻辑
不要改：
- `gated reconstruction`
- `cross_view_interaction`
- 导入逻辑
- `TriViewVMambaBlock`
- `RTHDBlock`

### 2. 保持测试语义清晰
测试结果要能明确区分：
- 纯重建模块测试
- 真实 `SS2D` 路径测试
- 无 CUDA 时跳过的测试

### 3. 不要假装通过
如果没有 CUDA，就明确写：
- 哪些测试被跳过
- 跳过原因是什么

---

## 修改后请自行验证

至少运行：

```bash
python3 -m py_compile umamba/docs0602/script/test_rthd_v1_enhancements.py
python3 umamba/docs0602/script/test_rthd_v1_enhancements.py
```

如果你在线上环境有 CUDA，也请实际运行并确认：
- 不再报 `Expected u.is_cuda() to be true, but got false`
- `TriViewVMambaBlock` 的测试能真正跑到真实 `SS2D`

---

## 输出要求

完成后请告诉我：

1. 你如何处理了 device
2. 哪些测试在 CPU 环境下会跳过
3. 哪些测试在 CUDA 环境下会真正执行
4. 修改后测试结果
5. 是否已经不再报 `u.is_cuda() to be true, but got false`
```