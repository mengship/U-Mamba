请只修一个问题：fallback placeholder 的输入输出格式。

当前问题：
- `SS2D` 导入失败时，`vmamba_2d / vmamba_axial / vmamba_coronal / vmamba_sagittal`
  被替换成了 `LayerNorm + Linear + GELU`
- 这类 placeholder 期望输入是 channels-last，即 `(B, H, W, C)`
- 但 `_process_view()` 在 `self.using_real_ss2d == False` 时没有做 `permute`
- 结果把 `(B, C, H, W)` 直接送进了 `LayerNorm(dim)`，测试仍报错

请修复要求：
1. 如果 fallback placeholder 使用的是 `LayerNorm + Linear` 这种 channels-last 结构，
   那么 `_process_view()` 在 fallback 分支也必须先把输入转成 `(B, H, W, C)`，
   输出后再转回 `(B, C, H, W)`。
2. 不要再依赖 `self.using_real_ss2d` 来决定格式，而应该根据“当前实际 placeholder 的格式需求”来处理。
3. 最简单做法是：
   - 统一让真实 `SS2D` 和当前 fallback `LayerNorm + Linear` 都走 channels-last
   - 前向结束后统一转回 channels-first
4. 只改 `umamba/nnunetv2/nets/rthd_modules.py`，不要扩大范围。

修改后请自行运行：
- `python3 -m py_compile umamba/nnunetv2/nets/rthd_modules.py umamba/docs0602/script/test_rthd_v1_enhancements.py`
- `python3 umamba/docs0602/script/test_rthd_v1_enhancements.py`

并报告结果。