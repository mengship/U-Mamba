你现在在 U-Mamba 项目中工作。请对当前 RTHD 实验代码做一次严格代码审计，目标是解释为什么以下三个实验在 fold 0、150 epochs 下 Mean Validation Dice 非常接近，且 RTHD 没有超过原始 U-Mamba：

1. nnUNetTrainerUMambaEnc_150epochs
   Mean Validation Dice: 0.8861286963708119

2. nnUNetTrainerUMambaEncRTHD_EncoderOnly_150epochs
   Mean Validation Dice: 0.8852501410983585

3. nnUNetTrainerUMambaEncRTHD_StageAwareDecoder_150epochs
   Mean Validation Dice: 0.884746558144406

请先审计，不要直接重构，不要大改代码。除非发现明确 bug，否则只输出分析和最小修复建议。

一、重点检查文件

1. umamba/nnunetv2/nets/UMambaEnc_3d.py
2. umamba/nnunetv2/nets/UMambaEnc_RTHD.py
3. umamba/nnunetv2/nets/rthd_modules.py
4. umamba/nnunetv2/training/nnUNetTrainer/nnUNetTrainerUMambaEnc.py
5. umamba/nnunetv2/training/nnUNetTrainer/nnUNetTrainerUMambaEnc_150epochs.py
6. umamba/nnunetv2/training/nnUNetTrainer/nnUNetTrainerUMambaEncRTHD_EncoderOnly.py
7. umamba/nnunetv2/training/nnUNetTrainer/nnUNetTrainerUMambaEncRTHD_EncoderOnly_150epochs.py
8. umamba/nnunetv2/training/nnUNetTrainer/nnUNetTrainerUMambaEncRTHD_StageAwareDecoder.py
9. umamba/nnunetv2/training/nnUNetTrainer/nnUNetTrainerUMambaEncRTHD_StageAwareDecoder_150epochs.py

二、审计目标

请重点确认以下问题：

1. Baseline 是否是真正的原始 U-Mamba
   - nnUNetTrainerUMambaEnc_150epochs 是否继承 nnUNetTrainerUMambaEnc
   - 是否调用 UMambaEnc_3d.py 的 get_umamba_enc_3d_from_plans
   - 是否没有意外使用 UMambaEnc_RTHD.py

2. EncoderOnly 是否真正启用了 RTHD encoder
   - 是否调用 get_umamba_enc_rthd_3d_from_plans
   - use_rthd_decoder=False 或 decoder_rthd_mode="none" 是否真的让 decoder 回退到标准 UNetResDecoder
   - rthd_config_encoder 是否正确传入
   - encoder 中哪些 stage 使用 RTHDBlock，是否与预期一致

3. StageAwareDecoder 是否真正启用了 partial decoder RTHD
   - decoder_rthd_mode="partial" 是否生效
   - rthd_stages_decoder=[0,1] 是否只作用在低分辨率 decoder stage
   - decoder 中 stage index 与论文描述 D4/D3 是否对应正确
   - StageAwareDecoder 是否没有意外启用 skip gate、boundary attention、frequency refinement

4. RTHDBlock 本身是否存在实现问题
   请重点检查：
   - TriViewProjection 是否把 3D 特征压成 2D 时丢失过多信息
   - projection_mode 默认值是什么
   - reconstruction_mode="gated" 是否真正生效
   - gated reconstruction 的 softmax 维度是否正确
   - share_weights=True 时是否真的三个视图共享同一个 2D VMamba
   - share_weights=False 分支是否只用于消融
   - cross_view_interaction=True 时 interaction_gate_conv 是否初始化和调用正确
   - RTHDBlock 的残差连接是否正确
   - 输入输出 shape 是否始终保持 (B,C,D,H,W)

5. 与原始 U-Mamba 的对比是否公平
   - baseline 和 RTHD 模型是否使用相同训练轮数
   - 是否都使用同一个 fold
   - 是否都使用相同 nnUNetPlans、patch size、batch size、数据增强、loss
   - 是否存在 AMP/NoAMP 不一致的问题
   - 注意：此前 AMP 训练出现 train_loss nan，NoAMP 正常。请检查这三个实验是否都采用相同精度策略。如果不一致，请明确指出。

6. summary.json 的 Mean Validation Dice 是否可比
   - nnU-Net 的 foreground_mean Dice 是不是 WT、TC、ET 的平均
   - 请建议提取 WT/TC/ET 三个区域 Dice 分别对比，而不要只看 Mean Dice
   - 如果可以，请给出一个读取 summary.json 并打印 WT/TC/ET/Mean Dice 的小脚本

7. 是否存在 RTHD 实际退化为近似普通模块的问题
   - 如果 SS2D 导入失败，是否使用了 placeholder fallback
   - 如果 fallback 被使用，是否会导致 RTHD 实际不是 VMamba
   - 请检查日志/代码中如何判断是否成功导入真实 SS2D
   - 请指出训练时应该在日志里看到哪些提示

三、请输出以下内容

1. 审计结论：
   - baseline 是否正确
   - EncoderOnly 是否正确
   - StageAwareDecoder 是否正确
   - RTHD 模块是否有明显 bug
   - 三个实验结果接近是否可能是正常现象

2. 发现的问题列表：
   按严重程度排序：
   - 严重 bug
   - 可疑实现
   - 实验不公平因素
   - 论文解释风险

3. 如果发现明确 bug：
   - 给出最小修改方案
   - 不要重构
   - 不要改无关文件

4. 如果没有明确 bug：
   - 给出下一步实验建议：
     a. 提取 WT/TC/ET 子区域 Dice
     b. 跑 SkipCalibration 和 Full
     c. 统计参数量、推理时间、显存
     d. 必要时尝试 Encoder RTHD stage 配置消融，例如只在部分 stage 使用 RTHD

5. 给出一个 summary.json 指标提取脚本：
   输入多个 summary.json 路径，输出：
   - WT Dice: mean["(1, 2, 3)"]["Dice"]
   - TC Dice: mean["(2, 3)"]["Dice"]
   - ET Dice: mean["(3,)"]["Dice"]
   - Mean Dice: foreground_mean["Dice"]

四、注意事项

- 不要删除已有文件
- 不要改变训练结果目录
- 不要默认修改核心网络
- 不要引入新依赖
- 如果需要修改，只做最小补丁，并说明原因